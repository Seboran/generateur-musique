{
  "name": "lib-effondrement",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/playground.ts --watch src",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "build": "tsc",
    "prepublish": "ctix create --overwrite --useRootDir && tsc"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "@types/prettier": "^2.7.3",
    "ctix": "^1.8.3",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1"
  },
  "files": [
    "dist"
  ]
}
