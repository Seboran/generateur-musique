{
  "name": "effondrement-onde-musique",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "dev": "pnpm -r --parallel --filter='./packages/*' run dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "pnpm -r --filter='./packages/*' run coverage",
    "test:playwright": "pnpm -r --filter='./packages/*' run test:playwright",
    "format": "prettier --write --cache ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.1",
    "vitest": "^0.34.0"
  },
  "dependencies": {
    "lib-effondrement": "workspace:*"
  }
}
