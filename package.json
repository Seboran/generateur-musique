{
  "name": "effondrement-onde-musique",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "dev": "pnpm -r --parallel --filter='./packages/*' run dev",
    "test": "pnpm -r --filter='./packages/effondrement' run test",
    "coverage": "pnpm -r --filter='./packages/*' run coverage",
    "test:playwright": "pnpm -r --filter='./packages/visualisation' run test:playwright",
    "format": "prettier --write --cache ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.1"
  },
  "dependencies": {
    "effondrement": "workspace:*"
  }
}
